/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['d1b6pcs0bxf31v.cloudfront.net', 'images.unsplash.com', 'ui-avatars.com','plus.unsplash.com'],
    },
};

export default nextConfig;
